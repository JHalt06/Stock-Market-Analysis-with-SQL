#The ingest directory is where the Pyhton will pull data from the API and push it to the PostgresSQL database.

#Load the API key from .env
#Talk to the API
#Connect to database
#Covnert raw API responses into the correct format for the tables.

#Fetch data for one stock (like Microsoft, Apple, etc)
#Print the raw JSON to make sure the API works